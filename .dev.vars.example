# Used only by Wrangler on localhost. Never enable LOCAL_ADMIN in production.
LOCAL_ADMIN=true

# Optional. Photo URLs use /media on the current origin by default.
# MEDIA_PUBLIC_URL=https://example.com/media
